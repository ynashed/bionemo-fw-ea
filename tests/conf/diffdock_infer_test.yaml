defaults:
  - infer

hydra:
  searchpath:
     - ${oc.env:BIONEMO_HOME}/examples/molecule/diffdock/conf

tmp_path: ???

seed: 42
out_dir: ${tmp_path}
samples_per_complex: 4

confidence_infer:
  trainer:
    deterministic: warn

score_infer:
  trainer:
    deterministic: warn
